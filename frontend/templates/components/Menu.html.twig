{% for menuItem in this.items %}
    <li class="nav-item">
        <a href="{{ menuItem.Odkaz.url ?? menuItem.Odkaz.sekceSlug  }}" class="nav-link px-2">{{ menuItem.Nadpis }}</a>
    </li>
{% endfor %}
